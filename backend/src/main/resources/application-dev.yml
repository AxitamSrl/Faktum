spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/faktum?stringtype=unspecified
    username: faktum
    password: REDACTED
    driver-class-name: org.postgresql.Driver

  data:
    redis:
      host: localhost
      port: 6900

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.faktum: DEBUG
    org.hibernate.SQL: DEBUG

meilisearch:
  enabled: true
  api-key: REDACTED

spring.security.oauth2.resourceserver.jwt:
  issuer-uri: https://sso.axitam.net/application/o/faktum/
